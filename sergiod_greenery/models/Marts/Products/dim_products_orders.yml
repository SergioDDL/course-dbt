version: 2

models:

   - name: dim_products_orders
     description: list of all products
     columns:
      - name: name
        tests:
          - unique
          - not_null
      - name: price
        tests:
          - not_null
      - name: inventory
      - name: orders_per_product