version: 2

models:

   - name: orders
     description: list of all products
     columns:
      - name: name
        tests:
          - unique
          - not_null
      - name: price
        tests:
          - not_null
      - name: inventory
      - name: orders_per_product